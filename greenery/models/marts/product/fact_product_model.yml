version: 2

models:
  - name: fact_product
    description: 'fact table with daily events per product'
    columns:
      - name: product_id 
        tests:
          - unique
          - not_null
      - name: page_views
      - name: add_to_carts
      - name: total_daily_orders