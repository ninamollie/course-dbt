version: 2

models:
  - name: fact_conversion_rate
    description: calculations of overall conversion rates 
    
    columns:
      - name: events_product_id

      - name: event_id

      - name: order_items_product_id

      - name: name

      - name: event_session_checkout

      - name: quantity
