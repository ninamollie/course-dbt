version: 2

models: 
- name: int_product_order
  description: Products per order daily
  columns:
    - name: date
      tests:
      - not_null
    - name: product_id
      tests:
      - not_null
      - unique